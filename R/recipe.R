# @importFrom recipes recipe
# @rdname recipe
# @export
# recipe.portfolio <- function(x, ...) {
#   out <- list(
#     #var_info = var_info,
#     #term_info = var_info,
#     steps = NULL,
#     template = x,
#     levels = NULL,
#     retained = NA
#     #requirements = requirements
#   )
#   class(out) <- c("recipe_portfolio", "recipe")
#   out
# }
